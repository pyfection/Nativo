# Required
DATABASE_URL=postgresql://nativo:nativo@localhost:5432/nativo
SECRET_KEY=replace-with-output-of-openssl-rand-hex-32

# Optional (defaults shown)
DEBUG=0
APP_NAME=Nativo
ACCESS_TOKEN_EXPIRE_MINUTES=30
# Comma-or-bracket-list of allowed origins. Use ["*"] for fully-open dev only.
BACKEND_CORS_ORIGINS=["http://localhost:5173"]
